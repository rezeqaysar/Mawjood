{
  "description": "Mawjood — voice-first family memory app (monorepo)",
  "name": "mawjood",
  "private": true,
  "scripts": {
    "mobile": "npm --workspace apps/mobile run start",
    "typecheck": "tsc --noEmit -p packages/voice-engine",
    "lint": "expo lint"
  },
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "react": "19.2.3",
    "react-dom": "19.2.3"
  }
}
